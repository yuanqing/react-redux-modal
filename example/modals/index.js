import ConfirmationModal, {CONFIRMATION_MODAL} from './confirmationModal';

// Map each `modalType` to the corresponding modal component.
export default {
  [CONFIRMATION_MODAL]: ConfirmationModal
};
